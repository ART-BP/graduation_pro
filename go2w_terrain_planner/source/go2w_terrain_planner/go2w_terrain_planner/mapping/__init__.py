"""Map generation, preprocessing, and temporal-fusion modules."""

# Import concrete implementations from their defining modules. Keeping this
# package initializer empty avoids importing the complete lidar and terrain
# stack when only one lightweight mapping utility is needed.
