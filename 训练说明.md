## 一、本地打包项目

cd /home/zezhao/graduation_project

export PACKAGE_NAME="go2w_terrain_planner_raycast_$(date +%Y%m%d_%H%M%S).tar.gz"

tar -czf "/tmp/$PACKAGE_NAME" \
  --exclude='go2w_terrain_planner/data' \
  --exclude='go2w_terrain_planner/outputs' \
  --exclude='go2w_terrain_planner/.pytest_cache' \
  --exclude='go2w_terrain_planner/**/__pycache__' \
  --exclude='go2w_terrain_planner/**/*.pyc' \
  go2w_terrain_planner

cd /tmp
sha256sum "$PACKAGE_NAME" > "$PACKAGE_NAME.sha256"

ls -lh "$PACKAGE_NAME" "$PACKAGE_NAME.sha256"

tar -tzf "/tmp/$PACKAGE_NAME" | sed -n '1,40p'

## 二、上传到服务器
用户名：easyai、root
密码：E3w3ui9h


ssh linux@ubuntu-66101 'mkdir -p ~/packages'
ssh -p 32062 easyai@i.easy-ai.cloud 'mkdir -p ~/packages'

scp -P 32062 "/tmp/$PACKAGE_NAME" \
    "/tmp/$PACKAGE_NAME.sha256" \
    easyai@i.easy-ai.cloud:~/packages/
scp "/tmp/$PACKAGE_NAME" \
    "/tmp/$PACKAGE_NAME.sha256" \
    easyai@i.easy-ai.cloud:~/packages/

## 三、服务器校验并解压
登录服务器：
ssh linux@ubuntu-66101
找到刚上传的文件：
cd ~/packages
ls -lt go2w_terrain_planner_raycast_*.tar.gz
设置包名，替换为实际文件名：
export PACKAGE_NAME="go2w_terrain_planner_raycast_20260919_XXXXXX.tar.gz"
校验文件：
sha256sum -c "$PACKAGE_NAME.sha256"
应显示：
go2w_terrain_planner_raycast_...tar.gz: OK
为避免与已有的第一版、第二版代码混淆，使用一个全新目录：
export GO2W_PROJECT_DIR="$HOME/projects/go2w_terrain_planner_v3"

mkdir -p "$GO2W_PROJECT_DIR"

tar -xzf "$HOME/packages/$PACKAGE_NAME" \
  -C "$GO2W_PROJECT_DIR" \
  --strip-components=1

cd "$GO2W_PROJECT_DIR"
检查关键文件：
find . -maxdepth 2 -type f | sort | sed -n '1,100p'
确认新的原生激光和地形文件存在：
test -f source/go2w_terrain_planner/go2w_terrain_planner/mapping/isaac_raycast_lidar.py
test -f source/go2w_terrain_planner/go2w_terrain_planner/mapping/pointcloud_grid_projector.py
test -f source/go2w_terrain_planner/go2w_terrain_planner/simulation/terrain_bank.py
test -f source/go2w_terrain_planner/go2w_terrain_planner/simulation/xt16_pattern.py

echo "关键文件检查通过"
检查旧模拟模块已经不存在：
find source -type f \
  \( -name 'simulated_local_map.py' \
  -o -name 'simulated_xt16_lidar.py' \
  -o -name 'simulated_pointcloud_projector.py' \)
该命令正常情况下没有输出。

## 四、设置训练数据目录
继续使用你当前的数据目录：
export GO2W_DATA_DIR=/home/easyai/projects/go2w_training
确认目录可写：
mkdir -p \
  "$GO2W_DATA_DIR/runs" \
  "$GO2W_DATA_DIR/smoke" \
  "$GO2W_DATA_DIR/evaluations" \
  "$GO2W_DATA_DIR/logs"

test -w "$GO2W_DATA_DIR" && echo "数据目录可写"
如果没有权限：
sudo mkdir -p "$GO2W_DATA_DIR"
sudo chown -R "$USER:$USER" "$GO2W_DATA_DIR"
训练数据不会进入Docker镜像，而是通过：
/home/easyai/projects/go2w_training → /workspace/data
挂载到容器中。

## 五、检查Docker和GPU
检查宿主机GPU：
nvidia-smi
检查Docker：
docker version
docker info | sed -n '1,80p'
检查NVIDIA Container Toolkit。不要再使用之前不兼容的CUDA 12.8测试镜像，可以等项目镜像构建后直接验证。
确认磁盘空间：
df -h
docker system df
建议至少保留约40～60GB可用空间。服务器已有Isaac Lab基础镜像时，Docker会复用缓存。

## 六、拉取Isaac Lab基础镜像
当前Dockerfile使用：
nvcr.io/nvidia/isaac-lab:2.3.2
执行：
docker pull nvcr.io/nvidia/isaac-lab:2.3.2
如果提示NGC鉴权：
docker login nvcr.io
用户名使用：
$oauthtoken
密码使用你自己的NGC API Key，不要把API Key写入脚本或代码仓库。

## 七、构建单元测试镜像
进入服务器代码目录：
cd "$GO2W_PROJECT_DIR"

export GO2W_IMAGE="go2w-terrain-planner:0.3.0"
export GO2W_TEST_IMAGE="go2w-terrain-planner:0.3.0-test"
先构建测试阶段：
docker build \
  --progress=plain \
  --target unit-test \
  --build-arg ISAAC_LAB_IMAGE=nvcr.io/nvidia/isaac-lab:2.3.2 \
  --build-arg GO2W_IMAGE_VERSION=0.3.0 \
  --build-arg GIT_COMMIT="$PACKAGE_NAME" \
  -t "$GO2W_TEST_IMAGE" \
  . 2>&1 | tee "$GO2W_DATA_DIR/logs/docker_build_unit_test.log"
构建过程中应该得到：
98 passed
如果单元测试失败，不要继续正式训练。

## 八、构建正式运行镜像
测试通过后：
docker build \
  --progress=plain \
  --target runtime \
  --build-arg ISAAC_LAB_IMAGE=nvcr.io/nvidia/isaac-lab:2.3.2 \
  --build-arg GO2W_IMAGE_VERSION=0.3.0 \
  --build-arg GIT_COMMIT="$(git rev-parse HEAD 2>/dev/null || echo unknown)" \
  -t "$GO2W_IMAGE" \
  . 2>&1 | tee "$GO2W_DATA_DIR/logs/docker_build_runtime.log"
检查镜像：
docker images "$GO2W_IMAGE"
docker inspect "$GO2W_IMAGE" \
  --format 'image={{.RepoTags}} created={{.Created}}'
再通过正式镜像运行一次单元测试：
docker run --rm \
  -e PYTHONDONTWRITEBYTECODE=1 \
  "$GO2W_IMAGE" \
  unit-test
应该仍然得到：
98 passed

## 九、验证容器GPU访问
使用刚构建的项目镜像验证，避免CUDA测试镜像与驱动版本不兼容：
docker run --rm \
  --gpus all \
  --entrypoint nvidia-smi \
  "$GO2W_IMAGE"
应该正确显示服务器GPU和24GB显存。
如果这里失败，问题属于宿主机驱动或NVIDIA Container Toolkit，暂时不要启动训练。

## 十、运行单环境GPU冒烟测试
先测试确定的楼梯阶段：
docker rm -f go2w-smoke 2>/dev/null || true

docker ps --format 'table {{.ID}}\t{{.Names}}\t{{.Image}}\t{{.Status}}'

docker run --rm \
  --name go2w-smoke \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e PYTHONDONTWRITEBYTECODE=1 \
  -e HYDRA_FULL_ERROR=1 \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  smoke \
  --device cuda:0 \
  --num_envs 1 \
  --steps 50 \
  --curriculum-min-stage 7 \
  --curriculum-max-stage 7
通过时应看到类似：
{
  "observation_source": "isaaclab_native_raycast",
  "point_slots_per_frame": 32000,
  "mean_lidar_point_count": 14980.0,
  "terrain_type_indices": [3],
  "physical_geometry_synchronized": true,
  "status": "passed"
}
结果保存在：
cat "$GO2W_DATA_DIR/smoke/smoke_test.json"
然后覆盖高难度阶段测试：
docker run --rm \
  --name go2w-smoke-hard \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e PYTHONDONTWRITEBYTECODE=1 \
  -e HYDRA_FULL_ERROR=1 \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  smoke \
  --device cuda:0 \
  --num_envs 8 \
  --steps 50 \
  --curriculum-min-stage 5 \
  --curriculum-max-stage 9
同时在另一个终端观察：
watch -n 1 nvidia-smi
重点检查：
没有CUDA OOM。
mean_lidar_point_count > 0。
observed_ratio > 0。
height_valid_ratio > 0。
physical_geometry_synchronized = true。
status = passed。
## 十一、短时间PPO压力测试
先用8个环境训练10代，并开启有限值检查：
docker rm -f go2w-train-stress 2>/dev/null || true

docker run --rm \
  --name go2w-train-stress \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e PYTHONDONTWRITEBYTECODE=1 \
  -e HYDRA_FULL_ERROR=1 \
  -e GO2W_RUNTIME_CHECKS=1 \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  train \
  --device cuda:0 \
  --num_envs 8 \
  --seed 42 \
  --max_iterations 10 \
  --curriculum-min-stage 5 \
  --curriculum-max-stage 9 \
  2>&1 | tee "$GO2W_DATA_DIR/logs/raycast_stress_test.log"
检查日志：
tail -n 100 "$GO2W_DATA_DIR/logs/raycast_stress_test.log"
确认：
没有NaN/Inf。
没有CUDA OOM。
Sensor/point_slots_per_frame 为32000。
Sensor/mean_point_count 大于0。
能够保存 model_0.pt 和 checkpoints/model_latest.pt。
注意：该压力测试固定采样课程5～9，不用于正式课程训练。
## 十二、正式从头训练
这次传感器和环境观测语义已经改变，不建议从第一版训练检查点使用 --resume。正式训练应从头开始一个新run。
建议先使用16个环境：
mkdir -p "$GO2W_DATA_DIR/logs"

tmux new -s go2w-raycast-v1
进入tmux后执行：
cd "$GO2W_PROJECT_DIR"

export GO2W_DATA_DIR=/home/easyai/projects/go2w_training
export GO2W_IMAGE=go2w-terrain-planner:0.3.0

docker rm -f go2w-raycast-train 2>/dev/null || true

docker run --rm \
  --name go2w-raycast-train \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e PYTHONDONTWRITEBYTECODE=1 \
  -e HYDRA_FULL_ERROR=1 \
  -e GO2W_RUNTIME_CHECKS=0 \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  train \
  --device cuda:0 \
  --num_envs 64 \
  --seed 42 \
  --max_iterations 10000 \
  2>&1 | tee "$GO2W_DATA_DIR/logs/raycast_v1_train.log"
不要添加课程范围参数。这样才会使用 configs/terrain.yaml 中完整的九阶段课程。
退出tmux但保持训练：
Ctrl+B
然后按 D
重新进入：
tmux attach -t go2w-raycast-v1
如果16环境稳定且显存余量充足，可以停止后重新从头使用32环境：
--num_envs 32
建议满足以下条件后再使用32环境：
峰值显存占用低于约21～22GB
没有OOM
steps/s稳定
地图有效率正常
## 十三、训练监控
查看容器：
docker ps
查看GPU：
watch -n 1 nvidia-smi
查看日志：
tail -f "$GO2W_DATA_DIR/logs/raycast_v1_train.log"
查找最新训练目录：
find "$GO2W_DATA_DIR/runs/rsl_rl/go2w_terrain_navigation" \
  -mindepth 1 \
  -maxdepth 1 \
  -type d \
  -printf '%TY-%Tm-%Td %TH:%TM  %f\n' |
sort
训练产物位于：
/data/go2w_training/runs/rsl_rl/go2w_terrain_navigation/
└── 时间戳_phase12_nine_stage_curriculum/
    ├── model_*.pt
    ├── checkpoints/
    │   └── model_latest.pt
    ├── configs/
    ├── params/
    ├── metadata.json
    └── events.out.tfevents.*
## 十四、训练中断后的续训
只允许续训本次新版本产生的检查点：
export RUN_DIR="$GO2W_DATA_DIR/runs/rsl_rl/go2w_terrain_navigation/实际运行目录"
export CHECKPOINT="$RUN_DIR/checkpoints/model_latest.pt"
执行：
docker run --rm \
  --name go2w-raycast-resume \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e PYTHONDONTWRITEBYTECODE=1 \
  -e HYDRA_FULL_ERROR=1 \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  train \
  --device cuda:0 \
  --num_envs 16 \
  --seed 42 \
  --max_iterations 10000 \
  --resume \
  --checkpoint "/workspace/data/${CHECKPOINT#$GO2W_DATA_DIR/}"
不要使用第一版或旧解析地图版本的checkpoint续训。
## 十五、训练后评估
设置容器内检查点路径：
export RUN_NAME="实际运行目录名"
export CONTAINER_CHECKPOINT="/workspace/data/runs/rsl_rl/go2w_terrain_navigation/$RUN_NAME/checkpoints/model_latest.pt"
评估全部课程：
docker run --rm \
  --name go2w-evaluate \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  evaluate \
  --device cuda:0 \
  --num_envs 16 \
  --steps 5000 \
  --checkpoint "$CONTAINER_CHECKPOINT" \
  --curriculum-min-stage 1 \
  --curriculum-max-stage 9
只评估课程5：
docker run --rm \
  --gpus all \
  --network host \
  --ipc host \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=Y \
  -e GO2W_DATA_ROOT=/workspace/data \
  -v "$GO2W_DATA_DIR:/workspace/data" \
  "$GO2W_IMAGE" \
  evaluate \
  --device cuda:0 \
  --num_envs 16 \
  --steps 5000 \
  --checkpoint "$CONTAINER_CHECKPOINT" \
  --curriculum-min-stage 5 \
  --curriculum-max-stage 5
评估结果保存在：
/data/go2w_training/evaluations/时间戳/metrics.json
这套流程中只有 go2w_terrain_planner 代码进入压缩包和Docker镜像；服务器已有的 /data/go2w_training 只作为运行时数据卷挂载，不会被打包或覆盖。


## 1. 检查服务器和显卡
cat /etc/os-release
uname -m
nvidia-smi --query-gpu=name,driver_version,memory.total --format=csv
df -h /
建议至少预留 60 GB 磁盘空间。
## 2. 更新 NVIDIA 驱动
先确保当前没有训练任务。驱动更新完成后必须重启服务器。
sudo apt update
sudo apt install -y ubuntu-drivers-common

ubuntu-drivers devices
apt-cache policy nvidia-driver-580
如果 nvidia-driver-580 存在候选版本：
sudo apt install -y nvidia-driver-580
sudo reboot
重新连接服务器后检查：
nvidia-smi
目标是显示：
Driver Version: 580.xx.xx
如果 apt-cache policy nvidia-driver-580 显示 Candidate: (none)，先不要下载 NVIDIA .run 安装包，把下面输出发给我：
cat /etc/os-release
nvidia-smi --query-gpu=name --format=csv,noheader
apt-cache search '^nvidia-driver-[0-9]+$'
## 3. 安装 Docker Engine
以下采用 Docker 官方软件源。Docker Ubuntu 安装文档
sudo apt update
sudo apt install -y ca-certificates curl

sudo install -m 0755 -d /etc/apt/keyrings

sudo curl -fsSL \
  https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc

sudo chmod a+r /etc/apt/keyrings/docker.asc
添加软件源：
sudo tee /etc/apt/sources.list.d/docker.sources >/dev/null <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF
安装 Docker：
sudo apt update

sudo apt install -y \
  docker-ce \
  docker-ce-cli \
  containerd.io \
  docker-buildx-plugin \
  docker-compose-plugin

sudo systemctl enable --now docker
sudo systemctl status docker --no-pager
允许当前用户直接运行 Docker：
sudo usermod -aG docker "$USER"
然后退出 SSH 并重新登录。重新登录后测试：
docker version
docker run --rm hello-world
注意：加入 docker 用户组相当于授予该用户较高的系统权限。
## 4. 安装 NVIDIA Container Toolkit
按照 NVIDIA 官方安装方式：NVIDIA Container Toolkit 安装文档
sudo apt update
sudo apt install -y ca-certificates curl gnupg2

curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey \
  | sudo gpg --dearmor \
      -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
添加软件源：
curl -s -L \
  https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list \
  | sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' \
  | sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
安装并配置：
sudo apt update
sudo apt install -y nvidia-container-toolkit

sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
验证 Docker GPU：
docker run --rm --gpus all \
  nvidia/cuda:12.6.3-base-ubuntu22.04 \
  nvidia-smi
这个命令必须在容器内部正确显示显卡型号、显存和驱动版本。
5. 下载 Isaac Lab 2.3.2 基础镜像
登录 NVIDIA NGC。用户名固定为 $oauthtoken，密码是你的 NGC API Key：
docker login nvcr.io
交互输入：
Username: $oauthtoken
Password: 你的 NGC API Key
然后下载镜像：
docker pull nvcr.io/nvidia/isaac-lab:2.3.2
检查镜像：
docker images | grep isaac-lab
验证基础镜像能够访问 GPU：
docker run --rm \
  --gpus all \
  --entrypoint nvidia-smi \
  nvcr.io/nvidia/isaac-lab:2.3.2
Isaac Lab 的 Docker 工作流依赖 Docker Engine、NVIDIA Container Toolkit 和 NVIDIA GPU 驱动。Isaac Lab Docker 文档


{
    "runtimes": {
        "nvidia": {
            "args": [],
            "path": "nvidia-container-runtime"
        }
    },
  "proxies": {
    "http-proxy": "http://172.16.2.254:8808",
    "https-proxy": "http://172.16.2.254:8808",
    "no-proxy": "localhost,127.0.0.1,::1,.easy-ai.cloud,172.16.,172.16.0.0/16"
    }
}
